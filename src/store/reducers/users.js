import {dummyUserObject} from '../../bootstrap/users'

const initialState = {
    users: dummyUserObject
};

const reducer = (state = initialState, action) => {
    return state
};

export default reducer;